// Minimal declaration to silence TypeScript for @tabler/icons-react
// We intentionally keep this broad (any) because the package doesn't provide type definitions
// and there is no @types package in DefinitelyTyped.

declare module '@tabler/icons-react';
